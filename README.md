# lab-mandat1

How to use

1. Clone this repo to get docker comnpose
1. Install [docker](https://docs.docker.com/engine/install/)
1. Start docker
    ```bash
    docker-compose up -d
    ```
2. To stop
    ```bash
    docker-compose down
    ```
